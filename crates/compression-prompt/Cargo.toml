[package]
name = "compression-prompt"
version = "0.1.0"
edition = "2021"
description = "Statistical compression for LLM prompts using intelligent filtering"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
tokenizers = "0.19"
anyhow = { workspace = true }
tracing = { workspace = true }
rand = { workspace = true }
